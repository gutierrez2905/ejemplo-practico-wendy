name: CI Simple Test

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Clonar repositorio
      uses: actions/checkout@v4

    - name: Verificar archivos del proyecto
      run: ls -la

    - name: Prueba completada con exito
      run: echo "¡El pipeline de CI corrio perfectamente, maestra!"